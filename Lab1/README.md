# Lab 1: Introduction to Linux, Command Execution, GCC, OSTEP Process API & Booting

## Overview
This directory contains the final report and supporting source code/screenshots for **Operating System Lab — Assignment 1**.

- **Student Name**: Rishi
- **Student ID**: 2024UCP1566
- **Institute**: Malaviya National Institute of Technology (MNIT), Jaipur

## Directory Structure
- `OS_Lab_2024UCP1566.docx`: Complete Lab Assignment 1 Report document.
- `ostep_code/`: Source code implementations of OSTEP Process API (`p1.c` to `p4.c`).
- `screenshots/`: Captured terminal outputs for directory, file, process, network, system, disk, and `/proc` commands.

## Key Concepts Covered
1. **Linux Architecture & Shell**: Architecture layering (Hardware → Kernel → Shell → Applications), types of shell (`bash`, `sh`, `csh`, `ksh`, `zsh`), and command syntax.
2. **Command Categories**: Executed and documented 50+ essential Linux commands in WSL Ubuntu.
3. **GCC/G++ Options**: Usage of flags (`-o`, `-Wall`, `-g`, `-O2`, `-c`, `-E`, `-S`).
4. **OSTEP CPU API**: Analysis of `fork()`, `wait()`, `execvp()`, and standard I/O redirection.
5. **Booting Process**: 5-stage booting sequence (POST → BIOS/UEFI → GRUB → Kernel → Systemd/Init).
